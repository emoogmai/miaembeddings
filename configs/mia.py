# N words before and after central word to predict (sequence context)
MIA_CONTEXT_LENGTH = 4

# Minimum number of times a word must appear in a text corpus in order to include it
# into the vocabulary
MIA_VOCABULARY_MINWORD_FREQUENCY = 50

# Maximun number of words in a sequence
MIA_MAXSEQUENCE_LENGTH = 256

# Output embedding dimensionality (how may elements the embeddings array will have)
MIA_EMBED_DIMENSION = 300

# Normalization parameter to control computed embeddings
MIA_EMBED_MAX_NORM = 1